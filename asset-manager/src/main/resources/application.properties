spring.datasource.url=jdbc:mysql://localhost:3306/asset_manager?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=lgybmha#12389
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 2. JPA / Hibernate Properties
# Changed MySQL8Dialect to just MySQLDialect for Hibernate 6/Spring Boot 4 compatibility.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 3. Security & Logging (Temporary for Setup)
# This allows us to access the site while we are still building the login logic.
logging.level.org.springframework.security=DEBUG

# 4. Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=titanff78@gmail.com
spring.mail.password=elkkcydabrxysary
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# 5. Server Configuration
server.port=8080